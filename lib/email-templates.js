const escape = (value) => String(value ?? "").replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character]);

export function emailTemplate({ eyebrow = "NEAT Ethical Investments", title, intro, details = [], cta, footer = "If you did not expect this message, you can safely ignore it." }) {
  const rows = details.map(([label, value]) => `<tr><td style="padding:8px 16px;color:#52779c;font-size:13px">${escape(label)}</td><td style="padding:8px 16px;color:#002b58;font-size:14px;font-weight:700">${escape(value)}</td></tr>`).join("");
  return `<!doctype html><html><body style="margin:0;background:#f4f8fc;font-family:Arial,sans-serif;color:#002b58"><table role="presentation" width="100%" cellspacing="0" cellpadding="0"><tr><td style="padding:32px 16px"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="max-width:620px;margin:auto;background:#fff;border-radius:20px;overflow:hidden"><tr><td style="padding:28px 32px;background:#002b58;color:#fff"><p style="margin:0 0 8px;font-size:12px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:#a9d5ff">${escape(eyebrow)}</p><h1 style="margin:0;font-size:27px;line-height:1.2">${escape(title)}</h1></td></tr><tr><td style="padding:30px 32px"><p style="margin:0;color:#385a7b;font-size:16px;line-height:1.6">${intro}</p>${rows ? `<table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="margin:24px 0;border:1px solid #dce8f5;border-radius:12px">${rows}</table>` : ""}${cta ? `<p style="margin:28px 0 0"><a href="${cta.href}" style="display:inline-block;background:#0057a6;color:#fff;text-decoration:none;padding:14px 20px;border-radius:10px;font-weight:700">${escape(cta.label)}</a></p>` : ""}<p style="margin:28px 0 0;color:#7188a0;font-size:12px;line-height:1.5">${escape(footer)}</p></td></tr></table></td></tr></table></body></html>`;
}

export function welcomeEmail(customer, portalUrl) { return emailTemplate({ title: `Welcome, ${customer.first_name}`, intro: "Your NEAT Ethical customer account is ready. You can use your portal to complete your profile, review activity, and make requests.", details: [["Account number", customer.account_number]], cta: { label: "Open customer portal", href: portalUrl } }); }
export function temporaryCredentialsEmail(customer, temporaryPassword, loginUrl) { return emailTemplate({ title: "Your NEAT account is ready", intro: "We created your customer account from your investment request. Use these temporary credentials to sign in, then set a private password on your first visit.", details: [["Email", customer.email], ["Temporary password", temporaryPassword], ["Account number", customer.account_number]], cta: { label: "Sign in to customer portal", href: loginUrl }, footer: "For your security, change the temporary password immediately after signing in. If you did not submit this request, contact NEAT Ethical." }); }
export function adminNotificationEmail({ title, text, details = [], portalUrl }) { return emailTemplate({ eyebrow: "NEAT operations", title, intro: escape(text), details, cta: portalUrl ? { label: "Open operations dashboard", href: portalUrl } : null }); }
