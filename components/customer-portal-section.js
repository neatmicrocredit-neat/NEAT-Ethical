"use client";

import Link from "next/link";
import { ArrowLeftRight, BriefcaseBusiness, CircleDollarSign, Landmark, LogOut, Menu, UserRound, X } from "lucide-react";
import { usePathname, useRouter } from "next/navigation";
import { useState } from "react";

const navigation = [
  { href: "/portal", label: "Overview", icon: Landmark },
  { href: "/portal/investments", label: "Investments", icon: BriefcaseBusiness },
  { href: "/portal/transactions", label: "Transactions", icon: ArrowLeftRight },
  { href: "/portal/loans", label: "Loans", icon: CircleDollarSign },
  { href: "/portal/profile", label: "My profile", icon: UserRound },
];

export function CustomerPortalSection({ customer, eyebrow = "Customer portal", title, description, children, action }) {
  const pathname = usePathname(); const router = useRouter(); const [mobileOpen, setMobileOpen] = useState(false);
  async function logout() { await fetch("/api/customer-auth/logout", { method: "POST" }); router.push("/auth/login"); }
  const nav = (mobile = false) => <nav className={mobile ? "border-t border-slate-100 bg-white px-5 py-3 lg:hidden" : "flex-1 px-4 py-7"}>{navigation.map(({ href, label, icon: Icon }) => { const active = pathname === href; return <Link key={href} href={href} onClick={() => setMobileOpen(false)} className={mobile ? `mr-2 inline-flex items-center gap-2 rounded-full px-3 py-2 text-xs font-black ${active ? "bg-blue-50 text-[#075ca8]" : "bg-slate-50 text-slate-600"}` : `mb-1 flex items-center gap-3 rounded-xl px-3.5 py-3 text-sm font-bold transition ${active ? "bg-blue-50 text-[#075ca8]" : "text-slate-500 hover:bg-slate-50 hover:text-slate-900"}`}><Icon className={mobile ? "size-3.5" : "size-4"} />{label}</Link>; })}</nav>;
  return <div className="min-h-screen bg-[#f5f8fc] text-slate-900"><aside className="fixed inset-y-0 left-0 z-40 hidden w-64 flex-col border-r border-slate-200 bg-white lg:flex"><div className="flex h-24 items-center border-b border-slate-100 px-7"><Link href="/portal" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-[#075ca8] text-white"><Landmark className="size-5" /></span><span><span className="block text-sm font-black tracking-tight text-slate-950">NEAT Ethical</span><span className="block text-[11px] font-bold text-slate-400">Customer portal</span></span></Link></div>{nav()}<div className="border-t border-slate-100 p-4"><div className="flex items-center gap-3 rounded-2xl bg-slate-50 p-3"><span className="grid size-9 place-items-center rounded-full bg-[#dbeafe] text-sm font-black text-[#075ca8]">{customer.first_name?.slice(0, 1).toUpperCase()}</span><span className="min-w-0"><span className="block truncate text-xs font-black text-slate-900">{customer.first_name} {customer.last_name}</span><span className="block truncate text-[11px] font-semibold text-slate-500">{customer.email}</span></span></div><button onClick={logout} className="mt-3 flex w-full items-center gap-3 rounded-xl px-3 py-2 text-sm font-bold text-slate-500 hover:bg-slate-50 hover:text-slate-900"><LogOut className="size-4" />Sign out</button></div></aside><div className="lg:pl-64"><header className="sticky top-0 z-30 border-b border-slate-200 bg-white/90 backdrop-blur"><div className="flex h-20 items-center justify-between gap-4 px-5 sm:px-8"><button onClick={() => setMobileOpen(!mobileOpen)} className="rounded-xl border border-slate-200 p-2.5 lg:hidden" aria-label="Toggle navigation">{mobileOpen ? <X className="size-5" /> : <Menu className="size-5" />}</button><div className="hidden sm:block"><p className="text-xs font-black uppercase tracking-[0.18em] text-[#075ca8]">{eyebrow}</p><p className="mt-1 text-sm font-semibold text-slate-500">Manage your NEAT Ethical account</p></div><div className="ml-auto flex items-center gap-3"><div className="hidden text-right sm:block"><p className="text-xs font-black text-slate-900">{customer.first_name} {customer.last_name}</p><p className="text-[11px] font-semibold text-slate-500">{customer.account_number || "Account pending"}</p></div><span className="grid size-10 place-items-center rounded-full bg-[#075ca8] text-sm font-black text-white">{customer.first_name?.slice(0, 1).toUpperCase()}</span></div></div>{mobileOpen ? nav(true) : null}</header><main className="mx-auto max-w-7xl px-5 py-8 sm:px-8 lg:px-10 lg:py-10"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-[#075ca8]">{eyebrow}</p><h1 className="mt-2 text-3xl font-black tracking-tight text-slate-950 sm:text-4xl">{title}</h1><p className="mt-3 max-w-2xl text-sm font-semibold leading-6 text-slate-500">{description}</p></div>{action || null}</div>{children}</main></div></div>;
}
